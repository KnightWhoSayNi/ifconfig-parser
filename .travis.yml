language: python
sudo: false
python:
  - "2.7"
  - "3.6"
env:
  global:
    - PACKAGENAME="ifconfig-parser"
before_install:
  - export PYTHONPATH=$PYTHONPATH:${pwd}:$(pwd)/ifconfig-parser:$(pwd)/ifconfig-parser/tests
install:
  - pip install -r requirements.txt
script:
  - python -m unittest -v unittest_ifconfig_parser.TestIfconfigParser